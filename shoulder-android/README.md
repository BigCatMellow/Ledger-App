# Shoulder Android

Android wrapper source for the standalone pork shoulder cook planner HTML supplied by the user. The app bundles the planner locally so it runs without a browser tab and can be installed as an Android APK.
